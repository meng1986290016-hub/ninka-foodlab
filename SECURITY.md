# 安全报告说明

## 支持范围

项目目前处于 `0.1.x` 预发布阶段，只为最新源码和最新预发布版本处理安全修复。旧构建可能不会单独回补。

## 如何报告

请不要在公开 Issue 中披露可利用的漏洞、真实 API Key、供应商资料、配方或备份内容。

仓库托管到 GitHub 并启用私密漏洞报告后，请使用仓库 **Security → Report a vulnerability** 提交。如果该入口尚未启用，请先通过项目维护者公开资料联系维护者，确认私下接收渠道后再发送技术细节；不要把漏洞样本或真实业务数据放入普通邮件主题、公开评论或聊天群。

报告建议包含：

- 受影响版本、操作系统和运行模式。
- 最小复现步骤、影响范围和必要的脱敏样本。
- 是否涉及本地数据库、备份、API Key、远程模型、MCP、导入文件或系统权限。
- 已知缓解方式；不要附带会访问第三方系统的验证脚本。

## 重点安全边界

- 桌面数据默认保存在本机，但 `.foodrd-backup` **未加密、未签名**。
- API Key 应只存在于操作系统安全凭据存储，不应写入数据库、日志或导出文件。
- 远程模型只应接收用户确认范围内的资料；模型、文档和 MCP 返回均视为不可信输入。
- 导入和恢复必须防止路径穿越、符号链接、重复条目和部分写入。
- 新增 Tauri capability、shell、网络或任意文件系统访问属于高风险变更，需要单独审阅。

普通功能缺陷、无法启动和数据兼容问题可以使用公开 Issue，但必须先删除真实配方、价格、供应商名称、附件和凭据。
