# 第二阶段供应商原料库验收清单

日期：2026-07-17

## 自动化结果

- [x] `@food-rd/core`：27 个测试通过。
- [x] `@food-rd/desktop`：43 个测试通过。
- [x] Rust：14 个测试通过，其中仓储层 11 个、命令层 3 个。
- [x] TypeScript 类型检查通过。
- [x] Vite 生产构建通过。
- [x] `cargo fmt --check` 通过。
- [x] `cargo clippy --all-targets -- -D warnings` 通过。

## 原料模型与操作

- [x] 内部编号为可选字段，默认列表不显示。
- [x] 标签字段已移除；自由文本仅保留在供应商版本的“研发备注”。
- [x] 可以创建、重命名和归档自定义分类。
- [x] 可以创建、编辑和归档供应商。
- [x] 同一通用原料可以保存多家供应商的独立版本。
- [x] 价格、型号/规格、密度、来源、备注、营养和最新更新日期属于供应商版本。
- [x] 保存成功后才更新 `updatedAt`；事务回滚不会留下半成品数据。
- [x] 可以按原料名、供应商等关键字搜索。
- [x] 可以复制、编辑和归档供应商版本。
- [x] 只能比较同一通用原料下的供应商版本。

## 营养数据

- [x] 内置能量、蛋白质、脂肪、饱和脂肪、碳水化合物、糖、膳食纤维和钠 8 项定义。
- [x] 支持“每 100 g”和“每 100 mL”基准。
- [x] 每 100 mL 换算到质量基准时要求密度。
- [x] 数据库和界面均保留 `null = 未知` 与字符串 `"0" = 已确认零值` 的差异。
- [x] 完整度按供应商版本字段计算，不把未知值当作零。

## 草稿与持久化

- [x] 浏览器演示数据使用 `food-rd.browser-demo.v2`。
- [x] schema v1 演示数据能迁移到分组的 schema v2，价格、密度、来源和备注不丢失。
- [x] 供应商版本草稿自动保存，可恢复或丢弃；正式保存后清除。
- [x] SQLite 首次启动执行 schema v1 迁移并写入 8 个内置营养定义。
- [x] 文件数据库关闭后重新打开，已提交分类与设置仍然存在。
- [x] 本机真实 Tauri 数据库重启前后迁移时间戳保持一致：`2026-07-16T18:22:43.224833+00:00`。
- [x] 本机数据库位置：`~/Library/Application Support/com.foodrd.studio/food-rd.sqlite3`。

## 桌面运行与安全

- [x] Rust stable、Tauri 2 开发进程可编译并启动真实应用。
- [x] 前端调用失败统一映射为 `{ code, message, field }` 结构化错误。
- [x] capability 只包含 `core:default`。
- [x] 未启用 shell 或不受限制的文件系统权限。
- [x] 当前 `bundle.active` 为 `false`，从源码运行不要求签名。

## 人工验证说明

真实 Tauri 开发窗口已成功启动，并确认其实际 SQLite 文件、迁移记录和重启行为。当前桌面自动化工具无法识别未打包的调试窗口，因此原生窗口内的逐字段点击由命令契约测试、仓储集成测试和浏览器同界面操作共同覆盖；未将这一限制写成“原生 UI 已全自动点击通过”。

## 推送后检查

- [ ] 确认 GitHub Actions 的 macOS job 通过。
- [ ] 确认 GitHub Actions 的 Windows job 通过。
- [ ] 在一台 Windows 机器上做一次图形界面人工启动与窄屏检查。
