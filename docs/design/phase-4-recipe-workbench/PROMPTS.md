# 配方工作台概念图生成记录

生成方式：内置 Image Gen  
用例分类：`ui-mockup`  
生成日期：2026-07-31

## 1. 桌面主工作台

目标：

- 延续食研配方现有白色 / 冷浅灰 / 绿色设计系统。
- 保留左侧导航、顶部数据库状态、Agent 入口和底部状态栏。
- 使用原料表作为主编辑面，右侧使用开放式实时结果检查器。
- 完整展示批量、用量、占比、锁定、补足、成本、目标、过敏原和一个 Markdown 研发备注框。
- 禁止仪表盘、KPI 卡、营销区、照片、渐变和无关功能。

关键文案：

- `配方工作台`
- `原味高蛋白酸奶`
- `草稿已自动保存`
- `保存为正式版本`
- `添加原料或半成品`
- `成本附加项`
- `研发备注`
- `实时结果`
- `数据完整度 92%`

输出：`desktop-workbench.png`

## 2. 原料选择状态

输入参考：`desktop-workbench.png`

目标：

- 在相同工作台上打开 620–680px 右侧抽屉。
- 明确选择具体供应商版本，而非只选通用原料。
- 供应商行显示型号、价格、关键营养、完整度和最新更新日期。
- 提供 `供应商原料` 与 `半成品版本` 页签。
- 使用表格行和分隔线，禁止卡片网格和 AI 推荐。

关键文案：

- `添加原料或半成品`
- `选择具体供应商原料版本或明确的半成品版本。`
- `搜索原料、供应商、型号或半成品`
- `供应商原料`
- `半成品版本`
- `已选择 1 项`
- `添加所选原料`

输出：`ingredient-picker.png`

## 3. 窄屏工作台

输入参考：`desktop-workbench.png`

目标：

- 生成约 760–820px 宽的窄窗口状态。
- 左侧导航收窄为 76px 图标栏。
- 工作台改为单列，结果通过 `配方`、`实时结果`、`目标与过敏原` 页签切换。
- 原料行保留排序、锁定、身份、用量、单位、占比、补足和数据问题。
- 成本与备注改为折叠区，底部显示投料和成本摘要。
- 禁止把右侧结果检查器压缩到主表旁边。

关键文案：

- `配方`
- `实时结果`
- `数据完整度 92%`
- `目标与过敏原`
- `成本附加项 2项`
- `研发备注`
- `投料 1000g · 成本 ¥20.72`
- `查看实时结果`

输出：`narrow-workbench.png`

## 4. 共同约束

- 纯白 `#ffffff`，冷浅灰 `#f6f8f7`，主绿色 `#087a43`。
- SF Pro Text / PingFang SC 风格。
- 12–14px 控件文字，24px 页面标题。
- 6–9px 圆角，表格和开放分隔区作为主要容器。
- 所有核心中文标签应清晰可读。
- 不生成水印。

